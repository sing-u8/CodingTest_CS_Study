-- Programmers SQL Template
-- https://school.programmers.co.kr/learn/courses/30/lessons/299307
-- 대장균의 크기에 따라 분류하기 1
-- 레벨3

SELECT ID,
       CASE
           WHEN SIZE_OF_COLONY <= 100 THEN 'LOW'
           WHEN SIZE_OF_COLONY > 100 AND SIZE_OF_COLONY <= 1000 THEN 'MEDIUM'
           WHEN SIZE_OF_COLONY > 1000 THEN 'HIGH'
           END AS SIZE
FROM ECOLI_DATA
ORDER BY ID ASC;


/*

 네, 작성하신 쿼리는 **표준 SQL 문법**을 아주 잘 지킨 **정석 답안**입니다!
가독성도 좋고, 오라클, MySQL, PostgreSQL 어디서든 동작하는 훌륭한 코드입니다.

하지만 실무에서 **코드를 더 간결하게 줄이거나**, **특정 DB(MySQL)의 함수**를 활용하는 다른 방법들도 있습니다. 크게 두 가지 팁을 드릴게요.

---

### 1. `CASE` 문 간소화 (가장 추천)

`CASE` 문은 위에서부터 순서대로 조건을 확인하고, **참(True)인 조건이 나오면 즉시 종료**됩니다.
이 원리를 이용하면 `AND` 조건과 마지막 `WHEN`을 생략하여 더 짧게 쓸 수 있습니다.

```sql
SELECT ID,
    CASE
        WHEN SIZE_OF_COLONY <= 100 THEN 'LOW'
        WHEN SIZE_OF_COLONY <= 1000 THEN 'MEDIUM' -- 앞서 100 이하가 걸러졌으므로, 여기는 자동으로 101~1000이 됩니다.
        ELSE 'HIGH'                               -- 나머지(1000 초과)는 모두 HIGH
    END AS SIZE
FROM ECOLI_DATA
ORDER BY ID ASC;

```

* **원리:** 첫 번째 줄(`<= 100`)이 거짓이어야만 두 번째 줄로 내려옵니다. 따라서 두 번째 줄에는 `SIZE > 100`이라는 조건을 굳이 적지 않아도 됩니다.
* **장점:** 로직 중복을 제거하여 코드가 깔끔해집니다.

---

### 2. MySQL 전용 함수 `IF` 사용 (코딩 테스트용)

만약 **MySQL** 환경이라면, 엑셀의 IF 함수처럼 중첩해서 사용할 수 있습니다. 코드가 매우 짧아지는 장점이 있습니다.

```sql
SELECT ID,
    IF(SIZE_OF_COLONY <= 100, 'LOW',
        IF(SIZE_OF_COLONY <= 1000, 'MEDIUM', 'HIGH')
    ) AS SIZE
FROM ECOLI_DATA
ORDER BY ID ASC;

```

* **구조:** `IF(조건, 참일때값, 거짓일때값)`
* **해석:**
1. 100 이하면 'LOW'
2. 아니라면? -> 다시 `IF`문 실행 -> 1000 이하면 'MEDIUM', 그것도 아니면 'HIGH'



---

### 💡 튜터의 결론

* **작성하신 코드:** 논리적으로 명확하고 안전한 코드입니다. (100점)
* **간소화된 CASE문:** 실무 개발자들이 가장 선호하는 스타일입니다. (추천)
* **IF 함수:** MySQL에서 빠르게 작성해야 할 때(코딩 테스트 등) 유용합니다.

상황에 맞춰 편한 방법을 사용하시면 됩니다! 아주 잘 풀으셨습니다.

 */